package com.first.mascotapp.fragments.viewModels

import android.view.View
import androidx.lifecycle.ViewModel
import com.first.mascotapp.R
import com.first.mascotapp.fragments.VetsFragment

class ServicesViewModel: ViewModel() {





    /*fun onBtnOtrosClicked(view: View) {
        val fragment = VetsFragment()
        val fragmentManager = requireActivity().supportFragmentManager
        val transaction = fragmentManager.beginTransaction()
        transaction.replace(R.id.serviceFragContainer, fragment)
        transaction.addToBackStack(null)
        transaction.commit()
    }*/
}